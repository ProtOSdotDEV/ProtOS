# ProtOS inittab - BusyBox init configuration

# System startup
::sysinit:/etc/init.d/rcS

tty1::respawn:/sbin/getty -n -l /etc/shell-login 0 tty1

ttyAMA0::respawn:/sbin/getty -n -l /etc/shell-login 115200 ttyAMA0

::shutdown:/bin/umount -a -r
::shutdown:/bin/sync
